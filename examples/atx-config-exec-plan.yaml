# DEPRECATED: This file is no longer needed as a separate config.
#
# All execution plan parameters now live in the `execution_plan` section of
# portfolio-config.yaml. The orchestrator reads them from there and generates
# the additionalPlanContext automatically.
#
# See: portfolio-config.schema.json for the full schema
# See: examples/reports/full-analysis/portfolio-config.yaml for a working example
#
# For manual execution without the orchestrator:
#   atx custom def exec -n portfolio-execution-plan-generation -p . -g file://portfolio-config.yaml -x -t
#
# The fields below show what used to be in this file — they now belong under
# `execution_plan:` in portfolio-config.yaml:
#
#   execution_plan:
#     team_size: 8
#     timeline_constraint: "12 months"
#     budget_constraint: "$1.2M including training and infrastructure"
#     compliance_requirements: ["SOC2", "PCI-DSS"]
#     availability_requirement: "99.95%"
#     risk_tolerance: "moderate"
#     existing_capabilities: "Strong Java/Spring, basic Docker, CI/CD with Jenkins, no Kubernetes experience"
