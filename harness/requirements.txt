# Change-Impact Harness — Python deps (see harness/DESIGN.md, .gitlab-ci.yml).
#
# The differ (diff-reports.py) and gate (should-run.sh) need NOTHING here — they use
# only the standard library, so local dev / unit tests run with a bare interpreter.
# These deps are for the two components that reach outside:
#
#   pyyaml  — read usecases.yaml (run-fixtures.sh helper, coverage-heatmap.py)
#   boto3   — Bedrock call in judge.py (falls back to an offline heuristic if absent)
#
#   pytest  — CI's harness:contract-tests job runs `python3 -m pytest harness/tests/`.
#             Each test file also has a built-in no-pytest fallback runner (`python3
#             harness/tests/test_x.py`) for a bare interpreter, but the CI job invokes
#             pytest directly, so it must be installed — not optional.
pyyaml>=6.0
boto3>=1.34
pytest>=8.0
